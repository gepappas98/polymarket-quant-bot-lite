python-dotenv>=1.0.0
requests>=2.31.0
rich>=13.0.0
ccxt>=4.0.0
# Uncomment for live CLOB trading:
# py_clob_client_v2>=0.1.0
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
httpx>=0.27
# celery>=5.3  # optional: background jobs; falls back to FastAPI BackgroundTasks

# Optional — strategy plugins (bot/strategies/), each off by default via .env:
# xgboost>=2.0.0                    # ML_STRATEGY_ENABLED (bot/ml_model.py)
# cryptography>=42.0.0              # KALSHI_ARB_ENABLED, RSA-PSS request signing (bot/venues/kalshi_client.py)
# prometheus-client>=0.20.0         # PROMETHEUS_PORT exporter (bot/metrics.py)
# psycopg[binary]>=3.1.0            # LEDGER_BACKEND=postgres
# psycopg_pool>=3.2.0               # LEDGER_BACKEND=postgres

# Dev/test only:
pytest>=8.0.0
